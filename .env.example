# Client-exposed vars must use the PUBLIC_ prefix in Astro
# (renamed from NEXT_PUBLIC_* in the Next.js project)
PUBLIC_WEATHER_API_KEY=
PUBLIC_GITHUB_CLIENT_ID=
PUBLIC_BASE_URL=http://localhost:3000

# Server-only (same names as before)
GITHUB_CLIENT_SECRET=
SMTP_USER=
SMTP_PASS=
EMAIL_TO=
